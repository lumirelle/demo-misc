# AGENTS.md

本仓库按日期组织内容。根目录下的每个 `YYYYMMDD` 文件夹代表一天，当天所有新增内容都放在对应日期的文件夹里。

## 新增文件规则

每次新增文件时，按以下步骤放置：

1. **确定日期文件夹**：取当天日期，格式为 `YYYYMMDD`（8 位数字，无分隔符，如 `20260824`）。该文件夹位于仓库根目录。
2. **不存在则创建**：若根目录下没有当天的 `YYYYMMDD` 文件夹，先创建它。
3. **放入主题子文件夹**：在日期文件夹内，按内容主题建一个子文件夹（如 `fonts-explore`、`hk-discussions-1220`），把新增文件放进该子文件夹。

完成标准：新增文件位于 `YYYYMMDD/<主题>/` 路径下，且 `YYYYMMDD` 与当天日期一致。

## 示例结构

```
20260823/
  fonts-explore/
  hk-discussions-1220/
20260824/
  code-fonts-explore/
```
